name: trackfile check

on:
  push:
    branches: [{{BRANCH}}]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npx --yes trackfile@latest check
